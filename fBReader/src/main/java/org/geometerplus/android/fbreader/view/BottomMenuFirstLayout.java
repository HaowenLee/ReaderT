package org.geometerplus.android.fbreader.view;

import android.content.Context;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import android.util.AttributeSet;
import android.widget.FrameLayout;

public class BottomMenuFirstLayout extends FrameLayout {

    public BottomMenuFirstLayout(@NonNull Context context) {
        this(context, null);
    }

    public BottomMenuFirstLayout(@NonNull Context context, @Nullable AttributeSet attrs) {
        this(context, attrs, 0);
    }

    public BottomMenuFirstLayout(@NonNull Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
